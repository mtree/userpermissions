import { ListIterablePipe } from './list-iterable.pipe';

describe('ListIterablePipe', () => {
  it('create an instance', () => {
    const pipe = new ListIterablePipe();
    expect(pipe).toBeTruthy();
  });
});
